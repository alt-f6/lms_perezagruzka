<%- include("../partials/head", { title: "Admin dashboard" }) %>
<%- include("../partials/header", { user: locals.user }) %>

<main class="mx-auto w-full max-w-5xl px-4 py-8">
  <div class="rounded-2xl border border-slate-800 bg-slate-950/50 p-6">
    <a href="/admin/users" class="mt-6 inline-block rounded-xl border border-slate-700 px-4 py-2 text-sm text-slate-200 hover:bg-slate-900">
      Manage users
    </a>
    <h1 class="text-3xl font-semibold">Admin dashboard</h1>

    <div class="mt-6 space-y-2 text-slate-300">
      <div>Hello, <span class="font-medium text-slate-100"><%= locals.user?.name || "Admin" %></span></div>
      <div>Role: <span class="font-medium text-slate-100"><%= locals.user?.role %></span></div>
    </div>
  </div>
</main>

<%- include("../partials/footer") %>
